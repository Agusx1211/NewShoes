#pragma once

// The original Visual C++ project was built on a case-insensitive filesystem.
// Keep the original source spelling intact while resolving it deterministically
// on the case-sensitive WebAssembly build host.
#include "StdAfx.h"
